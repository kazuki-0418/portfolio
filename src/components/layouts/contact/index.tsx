import { Outside } from "../../elements/Outside";
import { ContactPage } from "./ContactPage";

export const Contact = () => {
  return (
    <main className="h-screen bg-black" id="contact">
      {/* <Outside blend="difference"> */}
      <ContactPage />
      {/* </Outside> */}
    </main>
  );
};
